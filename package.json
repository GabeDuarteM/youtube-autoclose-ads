{
  "name": "youtube-autoclose-ads",
  "version": "1.0.0",
  "main": "src/script.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:clean && yarn build:parcel && yarn build:copy-public",
    "build:parcel": "parcel build",
    "build:copy-public": "copyfiles 'src/public/*' dist -f",
    "build:clean": "rimraf dist"
  },
  "dependencies": {
    "@types/chrome": "^0.0.56",
    "@types/node": "^8.5.2",
    "copyfiles": "^1.2.0",
    "parcel-bundler": "^1.3.1",
    "prettier": "^1.9.2",
    "rimraf": "^2.6.2",
    "tslint": "^5.8.0",
    "typescript": "^2.6.2"
  }
}
